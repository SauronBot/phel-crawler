version: "3.8"

services:
  crawler:
    build:
      context: .
      dockerfile: build/Dockerfile
    container_name: phel-crawler
    volumes:
      - .:/app:delegated
    # Override CMD to pass args:
    # docker compose run crawler https://example.com --max-pages 50
